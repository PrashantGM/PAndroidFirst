<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pandroidfirst">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PAndroidFirst">
        <activity android:name=".TimepickerActivity"></activity>
        <activity android:name=".DatepickerActivity" />
        <activity android:name=".DMASActivity" />
        <activity android:name=".SpinnerActivity">
        </activity>
        <activity android:name=".AlertDialogActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".GridActivity" />
        <activity android:name=".MessageActivity" />
        <activity android:name=".SiActivity" />
        <activity android:name=".MainActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        </application>

</manifest>